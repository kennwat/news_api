# План робіт для завершення тестового завдання

## 📊 Аналіз поточного стану проекту

### ✅ Що вже реалізовано

#### 1. Структура бази даних

-   ✅ Таблиця `users` (автори новин)
-   ✅ Таблиця `news` з підтримкою:
    -   Перекладів (title, short_description)
    -   Slug (унікальний)
    -   Видимість (is_visible)
    -   Дата публікації (published_at)
    -   Прив'язка до автора (user_id)
    -   Soft deletes
-   ✅ Таблиця `content_blocks` (блоки контенту з типами)
-   ✅ Таблиця `content_block_details` (деталі блоків: текст, зображення, позиція)
-   ✅ Правильні індекси для оптимізації запитів

#### 2. Моделі та бізнес-логіка

-   ✅ `User` модель
-   ✅ `News` модель з:
    -   Translations (via spatie/laravel-translatable)
    -   Relations (author, contentBlocks)
    -   Scopes (visible, published)
    -   Каскадне видалення блоків контенту
-   ✅ `ContentBlock` модель
-   ✅ `ContentBlockDetails` модель
-   ✅ `BlockTypeEnum` (Text, Image, TextImageRight, TextImageLeft, Slider)

#### 3. API Endpoints

-   ✅ Аутентифікація (Sanctum):
    -   POST `/api/register` - реєстрація
    -   POST `/api/login` - вхід
    -   POST `/api/logout` - вихід (auth)
-   ✅ Профіль (auth):
    -   GET `/api/profile` - перегляд профілю
    -   PUT/PATCH `/api/profile` - редагування профілю
-   ✅ Новини:
    -   GET `/api/news` - список новин (публічний + фільтрація)
    -   GET `/api/news/{slug}` - перегляд новини (публічний)
    -   POST `/api/news` - створення (auth)
    -   PUT/PATCH `/api/news/{slug}` - редагування (auth, owner)
    -   DELETE `/api/news/{slug}` - видалення (auth, owner)
    -   PATCH `/api/news/{slug}/toggle-visibility` - зміна видимості (auth, owner) ✅
    -   PATCH `/api/news/{id}/restore` - відновлення видаленої новини (auth, owner) ✅
    -   DELETE `/api/news/{id}/force` - повне видалення (auth, owner) ✅

#### 4. Form Requests

-   ✅ `RegisterRequest`
-   ✅ `LoginRequest`
-   ✅ `ProfileUpdateRequest`
-   ✅ `NewsStoreRequest`
-   ✅ `NewsUpdateRequest`

#### 5. Resources (API відповіді)

-   ✅ `NewsListResource`
-   ✅ `NewsResource`

#### 6. Policies

-   ✅ `NewsPolicy` (view, create, update, delete)

#### 7. Traits

-   ✅ `HasSlug` - генерація унікальних слагів
-   ✅ `CanLoadRelationships` - динамічне завантаження relations

#### 8. Features

-   ✅ Пошук по title та short_description (обидві мови)
-   ✅ Фільтрація по автору та даті публікації
-   ✅ Пагінація
-   ✅ Динамічне завантаження relations через `?include=author,contentBlocks.details`
-   ✅ Factories для моделей (User, News, ContentBlock, ContentBlockDetails)
-   ✅ Soft deletes з каскадним видаленням/відновленням

---

## 🚧 Що потрібно доробити

### 1. ⚠️ Критичні виправлення

#### 1.1 Додати відсутній метод `toggleVisibility` в `NewsController`

**Проблема:** Роут існує, але метод не реалізований

**Файл:** `app/Http/Controllers/Api/NewsController.php`

**Дія:**

```php
public function toggleVisibility(News $news): NewsResource
{
    Gate::authorize('update', $news);

    $news->update([
        'is_visible' => !$news->is_visible,
    ]);

    return new NewsResource($this->loadRelationships($news->fresh()));
}
```

#### 1.2 Додати метод `restore` для відновлення видалених новин

**Обґрунтування:** Потрібна можливість відновлення софт-видалених новин

**Файл:** `app/Http/Controllers/Api/NewsController.php`

**Дія:**

```php
public function restore(string $id): NewsResource
{
    $news = News::onlyTrashed()->findOrFail($id);

    Gate::authorize('restore', $news);

    $news->restore();

    return new NewsResource($this->loadRelationships($news->fresh()));
}
```

**Роут:** `routes/api.php`

```php
Route::patch('/news/{id}/restore', [NewsController::class, 'restore'])->name('news.restore');
```

#### 1.3 Додати метод `forceDelete` для повного видалення новин

**Обґрунтування:** Можливість повністю видалити новину з БД

**Файл:** `app/Http/Controllers/Api/NewsController.php`

**Дія:**

```php
public function forceDelete(string $id): JsonResponse
{
    $news = News::onlyTrashed()->findOrFail($id);

    Gate::authorize('forceDelete', $news);

    $news->forceDelete();

    return response()->json([
        'message' => 'News permanently deleted',
    ], Response::HTTP_NO_CONTENT);
}
```

**Роут:** `routes/api.php`

```php
Route::delete('/news/{id}/force', [NewsController::class, 'forceDelete'])->name('news.force-delete');
```

#### 1.4 Створити окремий контролер для публічної частини (опціонально)

**Обґрунтування:** Розділити логіку для автентифікованих та публічних користувачів

**Файл:** `app/Http/Controllers/Api/PublicNewsController.php` (вже створено термінал показує)

**Дії:**

-   Перенести логіку перегляду публічних новин
-   Спростити index метод для публічного доступу
-   Оновити роути

---

### 2. 🎨 Додати Filament Admin Panel

#### 2.1 Встановлення Filament

```bash
composer require filament/filament:"^3.0"
php artisan filament:install --panels
```

#### 2.2 Створити Filament Resources

```bash
php artisan make:filament-resource News --generate --view
php artisan make:filament-resource User --generate
```

#### 2.3 Що має включати Filament Admin:

-   **News Resource:**

    -   Управління новинами (CRUD)
    -   Відображення всіх полів (title, slug, short_description, image_preview_path)
    -   Управління блоками контенту (Repeater для content_blocks)
    -   Фільтрація: по автору, видимості, даті публікації
    -   Пошук: по назві та опису
    -   Bulk actions: видалення, зміна видимості
    -   Перегляд автора

-   **User Resource:**

    -   Список користувачів
    -   Перегляд новин користувача
    -   Базове управління

-   **Dashboard:**
    -   Статистика: кількість новин, користувачів
    -   Графіки публікацій
    -   Останні новини

#### 2.4 Налаштування доступу

-   Створити middleware/policy для адмінів
-   Або використати Filament Shield для ролей

---

### 3. 🧪 Написати тести (Pest)

#### 3.1 Встановлення Pest

```bash
composer require pestphp/pest --dev --with-all-dependencies
composer require pestphp/pest-plugin-laravel --dev
php artisan pest:install
```

#### 3.2 Структура тестів

**Feature тести:**

**A. Auth Tests** (`tests/Feature/AuthTest.php`)

-   ✅ Registration успішна
-   ✅ Registration з невалідними даними
-   ✅ Login успішний
-   ✅ Login з невірними credentials
-   ✅ Logout успішний

**B. Profile Tests** (`tests/Feature/ProfileTest.php`)

-   ✅ Перегляд власного профілю
-   ✅ Оновлення профілю
-   ✅ Неможливість доступу без аутентифікації

**C. News Tests** (`tests/Feature/NewsTest.php`)

-   **Публічний доступ:**

    -   ✅ Перегляд списку видимих новин (неавторизований)
    -   ✅ Перегляд окремої видимої новини
    -   ✅ Неможливість перегляду прихованої новини
    -   ✅ Пошук новин
    -   ✅ Фільтрація по автору та даті

-   **Автентифікований доступ:**
    -   ✅ Створення новини
    -   ✅ Створення новини з блоками контенту
    -   ✅ Автогенерація slug
    -   ✅ Перегляд власних новин (включно з прихованими)
    -   ✅ Оновлення власної новини
    -   ✅ Неможливість оновлення чужої новини
    -   ✅ Видалення власної новини
    -   ✅ Неможливість видалення чужої новини
    -   ✅ Toggle видимості новини
    -   ✅ Відновлення видаленої новини (restore)
    -   ✅ Повне видалення новини (force delete)
    -   ✅ Каскадне видалення блоків контенту

**D. Content Block Tests** (`tests/Feature/ContentBlockTest.php`)

-   ✅ Створення новини з різними типами блоків
-   ✅ Правильна позиція блоків
-   ✅ Створення блоків з деталями (slider)
-   ✅ Оновлення блоків при редагуванні новини
-   ✅ Видалення блоків разом з новиною

**E. Authorization/Policy Tests** (`tests/Feature/NewsPolicyTest.php`)

-   ✅ Власник може редагувати свою новину
-   ✅ Інший користувач не може редагувати чужу новину
-   ✅ Неавторизований користувач не може створювати новини
-   ✅ Всі можуть бачити видимі новини
-   ✅ Тільки власник може бачити приховані новини

**Unit тести:**

**A. Model Tests** (`tests/Unit/NewsModelTest.php`)

-   ✅ Relations працюють коректно
-   ✅ Scopes (visible, published) працюють
-   ✅ Translations працюють
-   ✅ Casts працюють

**B. Enum Tests** (`tests/Unit/BlockTypeEnumTest.php`)

-   ✅ Всі типи блоків доступні
-   ✅ Values() повертає правильний масив

---

### 4. 📝 Додаткові покращення (опціонально)

#### 4.1 Документація API

-   Встановити Scribe або L5-Swagger
-   Згенерувати OpenAPI документацію

#### 4.2 Seed дані

-   Оновити `DatabaseSeeder` для створення тестових даних
-   Додати команду для швидкого наповнення БД

#### 4.3 API Versioning

-   Винести API у версію v1: `/api/v1/news`

#### 4.4 Rate Limiting

-   Додати rate limiting для API endpoints

---

## 📋 Чеклист виконання

### Етап 1: Критичні виправлення

-   [x] Додати метод `toggleVisibility` в `NewsController`
-   [x] Додати метод `restore` в `NewsController` + роут
-   [x] Додати метод `forceDelete` в `NewsController` + роут
-   [x] Перевірити всі роути на наявність методів
-   [x] Запустити `vendor/bin/pint` для форматування коду

### Етап 2: Filament Admin Panel

-   [ ] Встановити Filament 3
-   [ ] Створити News Resource
-   [ ] Налаштувати форми з Repeater для блоків
-   [ ] Створити User Resource
-   [ ] Налаштувати Dashboard
-   [ ] Додати фільтри та пошук
-   [ ] Протестувати весь CRUD через адмінку

### Етап 3: Тести (Pest)

-   [ ] Встановити Pest
-   [ ] Конвертувати існуючі PHPUnit тести в Pest (якщо є)
-   [ ] Написати Auth тести (5 тестів)
-   [ ] Написати Profile тести (3 тести)
-   [ ] Написати News Feature тести (15+ тестів)
-   [ ] Написати Content Block тести (5 тестів)
-   [ ] Написати Policy тести (5 тестів)
-   [ ] Написати Unit тести для моделей (5 тестів)
-   [ ] Написати Unit тести для Enum (2 тести)
-   [ ] Запустити всі тести: `php artisan test`
-   [ ] Досягти мінімум 80% покриття основного функціоналу

### Етап 4: Фінальна перевірка

-   [ ] Запустити `vendor/bin/pint` для форматування
-   [ ] Запустити всі тести та переконатись що вони проходять
-   [ ] Перевірити всі API endpoints через Postman/Insomnia
-   [ ] Перевірити адмінку Filament
-   [ ] Оновити README.md з інструкціями

---

## 🎯 Пріоритети

### Високий пріоритет (обов'язково)

1. ✅ Додати метод `toggleVisibility`
2. ✅ Встановити Filament і створити базові Resources
3. ✅ Написати Feature тести для основного функціоналу

### Середній пріоритет (бажано)

1. ✅ Написати всі Unit тести
2. ✅ Налаштувати Dashboard в Filament
3. ✅ Додати Seed дані

### Низький пріоритет (nice to have)

1. API документація
2. API Versioning
3. Rate Limiting

---

## 📦 Додаткові пакети для встановлення

```bash
# Filament Admin Panel
composer require filament/filament:"^3.0"

# Pest Testing Framework
composer require pestphp/pest --dev --with-all-dependencies
composer require pestphp/pest-plugin-laravel --dev

# Опціонально (для API документації)
composer require knuckleswtf/scribe --dev
```

---

## ⏱️ Приблизний час виконання

-   **Етап 1 (Критичні виправлення):** 30 хв
-   **Етап 2 (Filament Admin):** 3-4 години
-   **Етап 3 (Тести Pest):** 4-6 годин
-   **Етап 4 (Фінальна перевірка):** 1 година

**Загальний час:** 8-11 годин

---

## 💡 Нотатки

### Переваги поточної реалізації:

-   ✅ Чиста архітектура з Form Requests
-   ✅ Правильне використання Policies
-   ✅ Soft deletes з каскадним видаленням
-   ✅ Підтримка перекладів через Spatie
-   ✅ Динамічна генерація slug
-   ✅ Оптимізовані індекси БД
-   ✅ Правильні Relations в моделях

### Що можна покращити:

-   Розділити NewsController на PublicNewsController та NewsController
-   Додати Cache для публічних новин
-   Додати Events/Listeners для аудиту змін
-   Додати Queue Jobs для важких операцій (якщо будуть)
-   Розширити валідацію для блоків контенту

---

## 🚀 Команди для швидкого старту

```bash
# Встановити залежності
composer install
npm install

# Запустити міграції
php artisan migrate:fresh --seed

# Встановити Filament
composer require filament/filament:"^3.0"
php artisan filament:install --panels

# Встановити Pest
composer require pestphp/pest --dev --with-all-dependencies
composer require pestphp/pest-plugin-laravel --dev
php artisan pest:install

# Створити Filament Resources
php artisan make:filament-resource News --generate
php artisan make:filament-resource User --generate

# Запустити тести
php artisan test

# Форматування коду
vendor/bin/pint

# Запустити сервер
php artisan serve
```

---

**Примітка:** Цей план покриває всі вимоги тестового завдання. Проект вже має солідну основу, тому залишилось додати адмінку (Filament) та тести (Pest).
